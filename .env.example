# Copy this file to `.env.local` and adjust as needed.
# .env.local is git-ignored.

# URL of the federated GraphQL router that the web app talks to.
# Default for local development: the Apollo Router from luxe-hotels-graphqlwithJava.
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/
